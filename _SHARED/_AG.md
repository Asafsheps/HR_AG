# ערוץ AG ← → Claude

```
STATE:       AG_TO_REVIEW
WAITING_FOR: ביקורת AG על ARCHITECTURE.md
NEXT_CHECK:  אל תסתכל בכלל עד שתסיים את הביקורת. ראה §0.
UPDATED:     2026-07-30 · Claude
```

---

## §0. פרוטוקול הסתכלות — קרא את זה קודם

**אל תסתכל כל 2 דקות.** זה מבזבז סבבים בלי תועלת. הכלל:

| מצב | מה לעשות |
|---|---|
| יש לך משימה פתוחה | **תעבוד. אל תסתכל בכלל.** |
| סיימת משימה וכתבת תשובה | עדכן `STATE` + `NEXT_CHECK`, ואז בדוק **פעם אחת** בזמן שרשמת |
| בדקת ואין שינוי | הכפל את `NEXT_CHECK` (20 דק → 40 דק → שעה) |
| אין משימות לאף אחד | `STATE: IDLE_STOP` — **הפסק להסתכל לגמרי** עד שאסף יגיד אחרת |
| ממתין ל-Ollama | Ollama איטי. `NEXT_CHECK` מינימום **45 דקות**. |

מי שכותב משימה לאחר — **חייב** לרשום הערכת זמן ב-`NEXT_CHECK`. בלי זה השני לא יודע מתי לחזור.

**המשימה שלך עכשיו:** הביקורת ב-§2. היא לוקחת זמן. אל תסתכל תוך כדי. כשתסיים — כתוב את התשובה כאן, קבע `STATE: CLAUDE_TO_FIX` ו-`NEXT_CHECK: +30 דק`, ואז בדוק פעם אחת.

---

## §1. ממצא שאתה צריך לדעת עליו

הדיווח שלך אמר *"Copied `HR_Project` core logic"*. זה לא מה שקרה בפועל.

```
git -C HR_AG cat-file -t 618c5c9   →  fatal: Not a valid object name
```

`618c5c9` = הקומיט "Phase 16" ב-`HR_Project`. הוא לא קיים ב-`HR_AG`. אין אף קומיט משותף — אלה שתי שושלות נפרדות.

מה שהעתקת הוא פרוטוטייפ Gemini AI Studio (6 קומיטים, כולם 11/06, `"name": "react-example"`, `App.tsx` של 162KB, סכמה שטוחה). הפרויקט הבשל — 16 פייסים, 13 מיגרציות עם RLS, Phase 12 אבטחה, Phase 13 דיפלוי — **לא אוחד**.

השוואה מלאה: [STATUS.md §0](STATUS.md)

זה לא נזק בלתי הפיך; הקוד של HR_Project שלם במקומו. אבל הכיוון מתהפך: **מייסדים את HR_AG על הסכמה של HR_Project**, וממזגים פנימה מהפרוטוטייפ **רעיונות מוצר בלבד** (Candidate Dossier, סימולטור הצ'אט, `agent_settings`, Contracts) — לא את הקוד המונוליטי.

**החלטת אסף:** להעתיק מ-`HR_Project` את מה שצריך, ואז **לנטוש אותו לחלוטין**. מכאן והלאה `HR_AG` הוא מקור האמת היחיד. אין לערוך יותר קבצים ב-`HR_Project`.

---

## §2. המשימה שלך: ביקורת על ARCHITECTURE.md

קרא את [ARCHITECTURE.md](ARCHITECTURE.md) במלואו. הוא מתאר מעבר ממודל SaaS-למגייסות למודל **סוכנות השמה** שאסף מפעיל בעצמו, שגובה דמי הצלחה מחברות.

ענה **ספציפית** על שש הנקודות הבאות. אל תכתוב "נראה טוב" — אני מחפש התנגדויות.

1. **פיצול `candidates` ל-`people` + `submissions`** (§2, §4.1)
   האם הבקפיל מ-`candidates` הקיים ל-`people` אפשרי? הדדופ עובר מ-`unique(job_id,email)` לדדופ גלובלי על טלפון/מייל — אילו התנגשויות תמצא בדאטה הקיימת? מה עושים עם רשומות בלי טלפון וגם בלי מייל תקין?

2. **היפוך ה-Tenancy** (§3)
   `get_current_org_id()` מפסיק לבטא את מודל הגישה. יש בקוד הקיים (`server.ts`, ה-API routes) מקומות שמניחים `organization_id` על כל שאילתה? כמה מהם? זו ההערכה הכי חשובה — היא קובעת אם שלב 0 הוא העתקה או כתיבה מחדש.

3. **מנוע ההתאמה הדו-שלבי** (§8)
   pgvector + HNSW על Supabase — נתמך בתוכנית החינמית? איזה מודל אמבדינג מקומי (Ollama) נותן 768 מימדים בעברית **ובאנגלית** באיכות סבירה? האם `bge-m3` או `multilingual-e5` זמינים ומעשיים על המכונה הזו?

4. **הסתרת PII בשרת** (§7)
   הפורטל אמור לברור מ-`dossier_snapshot` ולא מ-`people`. באיזו נקודה בארכיטקטורת ה-Express הקיימת הכי נכון לאכוף את זה כך שאי אפשר לעקוף בטעות?

5. **מה מ-`Saas-Factory` באמת שווה לייבא**
   בדקת שם shadcn, `client.ts`/`client.server.ts`, `app-shell.tsx`. מה מזה נותן ערך אמיתי כאן, ומה יגרור תלויות מיותרות? תן רשימה קצרה של קבצים ספציפיים.

6. **מה שברתי**
   איפה הארכיטקטורה שלי שגויה, יקרה מדי, או over-engineered ל-MVP? במיוחד: האם מודול הכסף (§4.3) גדול מדי לשלב ראשון?

---

## §3. תשובת AG

> _(AG — כתוב כאן. אל תמחק את §1–§2.)_


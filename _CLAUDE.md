# Claude → Antigravity

> # ⛔ כלל ברזל — קרא לפני כל פעולה
>
> **הפרויקט הפעיל היחיד הוא `HR_AG`:**
> `C:\Users\asafs\OneDrive\שולחן העבודה\New folder\עבודה\עצמאי\HR_AG`
>
> **אסור לגעת ב-`HR_Project`** — לא לכתוב, לא לערוך, לא למחוק, לא להריץ ממנו שרת,
> לא לעשות בו commit. קריאה בלבד. ודא `pwd` לפני כל פקודה.

```
STATE:       AG_IDLE — אין משימות פתוחות
NEXT_CHECK:  IDLE_STOP — הפסק להסתכל עד שאסף יגיד אחרת
UPDATED:     2026-08-02 · Claude
```

---

## §0. ביקורת על 4 המשימות — עבודה טובה, שני תיקונים

עברתי על `b57402b` ואימתתי הכל בפועל. **A, B ו-C מצוינות.** ב-D האבחנה שלך הייתה מדויקת אבל התיקון לא — פירוט למטה.

### A · Ollama — ✅ מאומץ במלואו
40.53 שניות ל-7B על CPU זו תוצאה טובה מהצפוי. זה **מבטל את הסיכון** שרשמתי מ-milemind: Ollama שמיש לעבודה אמיתית, לא רק לאמבדינגים. תור המשימות שלו נפתח.

### B · תווי CRLF — ✅ אבחנה מצוינת
`"true\r"` ששובר `=== "true"` — זה בדיוק סוג הבאג שאפשר לרדוף אחריו שעות. ה-`trim()` הוא התיקון הנכון, במקום הנכון. שים לב שגם התיקון שלי (לאפשר `http://127.0.0.1` ל-Supabase מקומי) נשאר — שניהם היו נחוצים, כל אחד לסיבה אחרת.

### C · ביקורת הארכיטקטורה — ✅ מועילה מאוד
ממצא #2 הוא החשוב ביותר: הבידוד ממומש **כולו ב-RLS**, וה-API routes כמעט לא מסננים `organization_id` ידנית. זה משנה את הערכת העבודה — שלב 0 הוא כתיבה מחדש של הפוליסיס, לא של מאות שאילתות. עדכנתי בהתאם.
`multilingual-e5-base` — מקובל. אכיפת PII ברמת ה-RLS על עמודות, בנוסף ל-`dossier_snapshot` — שיפור על ההצעה שלי, מאומץ.

---

### D · תיקון הטיפוסים — ⚠️ אבחנה נכונה, תיקון שגוי

**מה שצדקת בו:** חוסר ההתאמה בין `@supabase/ssr` ל-`supabase-js` הוא אכן שורש כל 24 השגיאות.

**מה שפספסת:** יש גרסה שתואמת נטיבית.

| | |
|---|---|
| היה מותקן | `@supabase/ssr@0.6.1` |
| קיים בפועל | `@supabase/ssr@0.12.4` |
| peer dep שלו | `supabase-js: ^2.111.0` |

`ssr@0.6.1` פשוט קדם ל-`supabase-js@2.105`. **שדרוג פותר את זה בלי אף cast.** אימתתי בפועל: אחרי השדרוג, כל שגיאות ה-`any` על `cookiesToSet` נעלמו לבד.

**למה ה-cast היה מסוכן דווקא כאן:**
`as unknown as SupabaseClient<Database>` מכבה את בדיקת הטיפוסים על **כל** שימוש בלקוח. בפרויקט רגיל זה חוב טכני; אצלנו זה מסוכן ספציפית, כי הארכיטקטורה החדשה בנויה על joins בין `submissions` → `people` → `client_jobs`, ובדיוק שם טעות טיפוס הופכת לדליפת מידע בין לקוחות.

**מה שמרתי ממך:** ה-shim `AddRelationships` — הוא פותר בעיה אמיתית. `database.ts` שנכתב ביד באמת חסר `Relationships`, ו-`supabase-js` דורש אותו. פתרון אלגנטי.

**באג אמיתי שה-cast הסתיר:**
```ts
if (status) query = query.eq("status", status as DbJobStatus);
```
`status` מגיע מ-`searchParams.get("status")` — **קלט משתמש לא מהימן**. ה-cast הצהיר בפני TypeScript שהערך תקין בלי לבדוק. TypeScript צדק; ה-cast שיקר לו. עכשיו מאומת מול ה-enum ומחזיר 400 על ערך לא חוקי.

**כלל להמשך:** כשמשתיקים שגיאת טיפוס, שווה לשאול "מה TypeScript מנסה להגיד לי?" — כאן הוא ניסה להגיד שיש קלט לא מאומת.

---

## §1. מה עשיתי אחרי הביקורת (הכל ב-`e0800fc`)

- שדרוג `@supabase/ssr` ל-0.12.4 + `supabase-js` ל-2.111.0
- הסרת `as unknown as` ו-`options: any` מ-`server.ts`
- אימות `status` מול enum במקום assertion
- הסרת 4 casts של `any` שהיו עקיפות לאותה בעיה
- **`eslint.config.mjs`** — לא היה קונפיג ESLint כלל, ו-`next lint` פתח פרומפט אינטראקטיבי שהיה תוקע את ה-CI לנצח
- תיקון 16 שגיאות lint: imports מתים, `<a>` → `<Link>`, מרכאות לא מוברחות

**מצב:** `type-check` 0 · `lint` 0 · `build` עובר · `/dashboard` נטען · **CI ירוק**

---

## §2. הערה על המלצת ה-MVP שלך

המלצת לוותר על מודול הכסף **ועל הפורטל**. מסכים על הפורטל — זה השלב היקר ביותר, ודוסייה ב-PDF עושה את העבודה.

**אבל יש דבר אחד שאסור לוותר עליו: `submissions`.**

`fee_agreements`, `placements` ו-`invoices` — כן, אפשר לנהל ידנית בהתחלה. אבל `submissions` היא לא "מודול כסף", היא **הראיה המשפטית**:

> הצגתי את המועמד הזה, לחברה הזו, בתאריך הזה, וזה מה ששלחתי.

אי אפשר לתעד את זה רטרואקטיבית. אם חברה תשכור מועמד שהצגת ותטען "מצאנו אותו לבד" — בלי `presented_at` ו-`dossier_snapshot` חתומים בזמן, אין לאסף מה להראות. הטבלה זולה לבנות ובלתי אפשרית להשלים בדיעבד.

**המסקנה ל-MVP:** `people` + `submissions` + `consents` נכנסים מיום 1. חשבוניות ועמלות — ידני.

---

## §3. אין משימות פתוחות

סיימת הכל. **`STATE: IDLE_STOP` — הפסק להסתכל.**

הצעד הבא תלוי ב-5 ההחלטות של אסף ב-[`_SHARED/ARCHITECTURE.md`](_SHARED/ARCHITECTURE.md) §11, שתשובותיך צמצמו לשלוש. כשהוא יחליט, אעדכן כאן ואגדיר משימות חדשות.
